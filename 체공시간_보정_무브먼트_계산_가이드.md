# Raw Statcast 데이터로 체공시간 보정 무브먼트 계산하기

이 문서는 Baseball Savant에서 내려받은 투구 단위 Statcast CSV로부터 팔 각도를 사용하지 않고 다음 두 값을 계산하는 독립 실행형 가이드다.

- `ivb_ft`: 기준 체공시간으로 환산한 수직 무브먼트(inch)
- `hb_ft`: 기준 체공시간으로 환산한 투수 기준 수평 무브먼트(inch, 암사이드 +)

여기서 `ft`는 feet가 아니라 **flight-time adjusted**의 약자다. 결과 단위는 inch다.

---

# 1부. 계산에 필요한 수식만 먼저 보기

## 1. 필요한 raw 칼럼

최소 계산 칼럼은 다음과 같다.

| raw 칼럼 | 단위·형식 | 용도 |
|---|---:|---|
| `pfx_z` | ft | 원 수직 무브먼트 |
| `pfx_x` | ft | 포수 시점 원 수평 무브먼트 |
| `p_throws` | `R`/`L` | 수평 무브먼트의 좌우 방향 통일 |
| `release_pos_y` | ft | 릴리스 지점의 y좌표 |
| `vy0` | ft/s | Statcast 기준점 (y=50)ft에서의 y축 속도 |
| `ay` | ft/s² | y축 평균 가속도 |
| `pitch_type` | 범주 | 구종별 기준 체공시간 연결 |
| `game_year` | 연도 | 기준 체공시간 학습기간 선택 |

현재 식에는 `arm_angle`, `release_speed`, `release_extension`, `ax`, `az`, `spin_axis`, `release_spin_rate`가 직접 들어가지 않는다.

## 2. 상수와 부호 정의

홈플레이트 앞면의 y좌표:

$$
y_P=\frac{17}{12}=1.416\overline{6}\text{ ft}
$$

Statcast 궤적 계수의 기준 y좌표:

$$
y_0=50\text{ ft}
$$

투수 손잡이 변환계수:

$$
s_i=
\begin{cases}
-1,&p\_throws_i=R\\[2mm]
+1,&p\_throws_i=L
\end{cases}
$$

## 3. 1단계 — raw 무브먼트를 inch로 변환

$$
\boxed{IVB_{raw,i}=12\,pfx_{z,i}}
$$

$$
\boxed{HB_{catcher,i}=12\,pfx_{x,i}}
$$

## 4. 2단계 — 수평 무브먼트를 투수 기준으로 통일

$$
\boxed{HB_{arm,i}=12\,s_i\,pfx_{x,i}}
$$

동일한 식을 손잡이별로 쓰면:

$$
HB_{arm,i}=
\begin{cases}
-12\,pfx_{x,i},&\text{우투수}\\[2mm]
+12\,pfx_{x,i},&\text{좌투수}
\end{cases}
$$

## 5. 3단계 — 특정 y 위치에 도달하는 시각 계산

Statcast y축 궤적식:

$$
y(t)=50+v_{y0,i}t+\frac12a_{y,i}t^2
$$

목표 위치 $y^*$에 도달하는 시각의 후보 근:

$$
\boxed{
t_i(y^*)=
\frac{-v_{y0,i}\pm
\sqrt{v_{y0,i}^{2}-2a_{y,i}(50-y^*)}}
{a_{y,i}}
}
$$

두 근 중 $t=0$에 더 가까운 물리적 근을 선택한다.

$a_{y,i}=0$에 매우 가까운 예외에서는:

$$
\boxed{t_i(y^*)=-\frac{50-y^*}{v_{y0,i}}}
$$

## 6. 4단계 — 릴리스부터 홈플레이트까지의 체공시간 계산

릴리스 시각:

$$
t_{R,i}=t_i(release\_pos\_y_i)
$$

홈플레이트 앞면 도착 시각:

$$
t_{P,i}=t_i\left(\frac{17}{12}\right)
$$

체공시간:

$$
\boxed{T_i=t_{P,i}-t_{R,i}}
$$

## 7. 5단계 — 구종별 기준 체공시간 계산

현재 연구를 그대로 재현할 때 학습연도 집합은:

$$
\mathcal{Y}_{train}=\{2021,2022,2023\}
$$

구종 $g$의 기준 체공시간:

$$
\boxed{
T_{ref,g}
=
\operatorname{median}
\left\{
T_i:
pitch\_type_i=g,
game\_year_i\in\mathcal{Y}_{train},
i\in\mathcal{E}
\right\}
}
$$

여기서 $\mathcal{E}$는 추적값과 체공시간이 유효한 기준 표본이다. 최소 권장 체공시간 범위는:

$$
\boxed{0.25\le T_i\le0.60\text{ s}}
$$

이다.

## 8. 6단계 — 체공시간 보정계수 계산

$$
\boxed{
C_i=
\left(
\frac{T_{ref,g}}{T_i}
\right)^2
}
$$

## 9. 7단계 — 최종 보정 무브먼트 계산

수직:

$$
\boxed{
IVB_{FT,i}
=IVB_{raw,i}C_i
=12\,pfx_{z,i}
\left(
\frac{T_{ref,g}}{T_i}
\right)^2
}
$$

수평:

$$
\boxed{
HB_{FT,i}
=HB_{arm,i}C_i
=12\,s_i\,pfx_{x,i}
\left(
\frac{T_{ref,g}}{T_i}
\right)^2
}
$$

## 10. 최종 산출 칼럼

| 산출 칼럼 | 단위 | 식 |
|---|---:|---|
| `ivb_raw_inches` | inch | $12pfx_z$ |
| `hb_raw_inches` | inch | $12pfx_x$, 포수 시점 |
| `hb_arm_side_inches` | inch | $12s_ipfx_x$, 암사이드 + |
| `flight_time_s` | s | $t_P-t_R$ |
| `reference_flight_time_s` | s | 학습자료 내 구종별 $T$ 중앙값 |
| `flight_time_scale` | 배율 | $\left(T_{ref}/T\right)^2$ |
| `ivb_ft` | inch | $IVB_{raw}\times flight\_time\_scale$ |
| `hb_ft` | inch | $HB_{arm}\times flight\_time\_scale$ |

---

# 2부. 각 수식이 필요한 이유

## 11. 왜 `pfx_x`, `pfx_z`에 12를 곱하는가?

Statcast CSV의 `pfx_x`, `pfx_z`는 ft 단위다. 투구 무브먼트는 보통 inch로 설명하므로 다음 단위변환을 한다.

$$
1\text{ ft}=12\text{ inch}
$$

예를 들어 `pfx_z = 1.5`라면:

$$
IVB_{raw}=1.5\times12=18\text{ inch}
$$

이 단계는 물리적 보정이 아니라 단순한 단위변환이다.

## 12. 왜 우투수의 수평 무브먼트에 −1을 곱하는가?

`pfx_x`는 포수 시점 좌표다. 같은 암사이드 움직임이라도 우투수와 좌투수에서는 좌우 방향이 반대이므로 원자료의 부호도 반대로 나타난다.

투수의 손잡이와 무관하게:

- 양수: 암사이드 런
- 음수: 글러브사이드 컷

이 되도록 우투수는 부호를 뒤집고 좌투수는 원래 부호를 유지한다. 이는 크기를 바꾸는 보정이 아니라 좌표계를 통일하는 과정이다.

## 13. 왜 `release_speed` 대신 `vy0`, `ay`를 사용하는가?

`release_speed`만 사용하면 공이 비행 중 항력 때문에 느려지는 현상을 반영하기 어렵다. 현재 계산은 Statcast가 제공하는 y축 속도와 평균 가속도를 사용한다.

$$
y(t)=50+v_{y0}t+\frac12a_yt^2
$$

이 식은 공이 마운드에서 홈플레이트 방향으로 이동하면서 y 위치가 시간에 따라 어떻게 변하는지를 나타낸다. `vy0`는 보통 음수이고 `ay`는 홈 방향 속도의 절댓값이 비행 중 감소하는 효과를 반영한다.

## 14. 왜 릴리스 시각이 음수일 수 있는가?

Statcast의 `vy0`, `ay`는 $y=50$ft 지점을 $t=0$으로 한 계수다. 실제 릴리스 지점은 일반적으로 $y=53\sim55$ft 부근이므로 공은 $t=0$보다 조금 전에 릴리스된다.

따라서 보통:

$$
t_R<0, t_P>0
$$

가 되며 실제 체공시간은:

$$
T=t_P-t_R>0
$$

가 된다.

## 15. 왜 이차방정식에서 두 근 중 하나만 선택하는가?

이차방정식에는 수학적으로 두 근이 생길 수 있다. 그러나 실제 Statcast 궤적은 $y=50$ft 부근을 한 번 지나 홈플레이트로 향하는 구간이다. 코드에서는 두 후보 중 $t=0$에 더 가까운 근을 선택한다. 판별식이 음수이거나 체공시간이 0 이하이면 잘못된 추적값으로 간주해 결측 처리한다.

## 16. 왜 `release_extension`을 직접 사용하지 않는가?

`release_pos_y`가 실제 릴리스 위치를 직접 제공한다. 릴리스가 홈에 가까워질수록 `release_pos_y`가 작아지고, 그 결과 계산된 체공시간도 짧아진다.

따라서 익스텐션의 효과는 다음 경로로 이미 반영된다.

$$
release\_pos\_y
\longrightarrow
t_R
\longrightarrow
T
\longrightarrow
C
$$

`release_extension`을 보정식에 다시 넣으면 같은 정보를 이중으로 반영할 가능성이 있다.

## 17. 왜 구종별 기준 체공시간을 사용하는가?

포심, 싱커, 커터는 일반적인 구속과 체공시간 분포가 다르다. 하나의 기준시간을 모든 구종에 적용하면 구종 자체의 평균적인 속도 차이가 보정값에 섞인다.

따라서:

$$
T_{ref,FF}, T_{ref,SI}, T_{ref,FC}
$$

를 별도로 계산한다. 중앙값을 쓰는 이유는 극단적인 체공시간에 평균보다 덜 민감하기 때문이다.

현재 연구의 2024·2025년 외부검증을 재현하려면 2021–2023년에서 계산한 기준시간을 고정하여 사용해야 한다. 대상 연도까지 포함해 기준시간을 다시 계산하면 미래 정보가 들어가고 연도 간 비교척도도 달라진다.

## 18. 왜 체공시간의 제곱으로 보정하는가?

공의 진행방향에 수직인 평균 가속도를 $a_\perp$, 체공시간을 $T$, 그 결과 생긴 무브먼트를 $M$이라고 하자. 횡방향 초기속도를 0으로 놓은 등가속도 운동에서는:

$$
M=\frac12a_\perp T^2
$$

이다. 관측된 공의 평균 횡가속도를 역산하면:

$$
a_\perp=\frac{2M}{T^2}
$$

이 공이 동일한 평균 횡가속도를 유지하면서 기준시간 $T_{ref}$만큼 날아갔다고 가정하면:

$$
M_{FT}=\frac12a_\perp T_{ref}^2
$$

여기에 $a_\perp=2M/T^2$를 대입하면:

$$
M_{FT}
=\frac12
\left(\frac{2M}{T^2}\right)
T_{ref}^2
=M\left(\frac{T_{ref}}{T}\right)^2
$$

가 된다. 즉 현재 식은 모든 투구를 동일한 체공시간으로 환산한다.

## 19. 보정계수의 직관적인 의미

보정계수는:

$$
C=\left(\frac{T_{ref}}{T}\right)^2
$$

이다.

- $T=T_{ref}$: $C=1$. 원 무브먼트와 보정 무브먼트가 같다.
- $T>T_{ref}$: $C<1$. 평균보다 오래 움직일 기회를 얻었으므로 하향 조정한다.
- $T<T_{ref}$: $C>1$. 평균보다 짧은 시간에 움직였으므로 상향 조정한다.

예를 들어 원 무브먼트가 16inch이고 $T=0.40$초, $T_{ref}=0.42$초라면:

$$
M_{FT}
=16\left(\frac{0.42}{0.40}\right)^2
=17.64\text{ inch}
$$

이다. 실제 이동거리가 17.64inch였다는 뜻은 아니다. 같은 평균 횡가속도가 기준시간 동안 작용했을 때의 **기준시간 환산값**이다.

---

# 3부. 바로 실행할 수 있는 Python 예제

아래 코드는 pandas와 NumPy만 사용한다. 입력 CSV는 투구당 한 행인 Baseball Savant 상세 CSV라고 가정한다.

```python
from pathlib import Path

import numpy as np
import pandas as pd


# -----------------------------
# 사용자 설정
# -----------------------------
CSV_PATH = Path("statcast.csv")
OUTPUT_PATH = Path("statcast_with_flight_adjusted_movement.csv")

TRAIN_YEARS = [2021, 2022, 2023]
PITCH_TYPES = ["FF", "SI", "FC"]
PLATE_Y_FT = 17.0 / 12.0
REFERENCE_Y_FT = 50.0
MIN_FLIGHT_TIME_S = 0.25
MAX_FLIGHT_TIME_S = 0.60


def time_at_y(vy0, ay, target_y, reference_y=50.0):
    """Statcast y=50 ft 기준 궤적에서 target_y 도달 시각을 계산한다."""
    vy = np.asarray(vy0, dtype=float)
    accel = np.asarray(ay, dtype=float)
    target = np.asarray(target_y, dtype=float)
    vy, accel, target = np.broadcast_arrays(vy, accel, target)

    c = reference_y - target
    discriminant = vy**2 - 2.0 * accel * c
    result = np.full(vy.shape, np.nan, dtype=float)

    finite = (
        np.isfinite(vy)
        & np.isfinite(accel)
        & np.isfinite(target)
        & (discriminant >= 0)
    )

    # ay가 사실상 0이면 직선운동식 사용
    linear = finite & np.isclose(accel, 0.0)
    result[linear] = np.divide(
        -c[linear],
        vy[linear],
        out=np.full(linear.sum(), np.nan),
        where=vy[linear] != 0,
    )

    # 일반적인 이차방정식
    quadratic = finite & ~linear
    sqrt_discriminant = np.sqrt(
        np.where(quadratic, discriminant, np.nan)
    )
    root_1 = (-vy - sqrt_discriminant) / accel
    root_2 = (-vy + sqrt_discriminant) / accel

    # y=50 ft의 t=0에 가까운 물리적 근 선택
    nearest_root = np.where(
        np.abs(root_1) <= np.abs(root_2), root_1, root_2
    )
    result[quadratic] = nearest_root[quadratic]
    return result


# -----------------------------
# 1. 데이터 읽기 및 필수 칼럼 확인
# -----------------------------
data = pd.read_csv(CSV_PATH, low_memory=False)

required = {
    "pfx_x",
    "pfx_z",
    "p_throws",
    "release_pos_y",
    "vy0",
    "ay",
    "pitch_type",
    "game_year",
}
missing = required.difference(data.columns)
if missing:
    raise KeyError(f"필수 칼럼이 없습니다: {sorted(missing)}")

numeric_columns = [
    "pfx_x", "pfx_z", "release_pos_y", "vy0", "ay", "game_year"
]
for column in numeric_columns:
    data[column] = pd.to_numeric(data[column], errors="coerce")

# 연구대상 구종만 남기고 싶을 때 사용한다.
data = data[data["pitch_type"].isin(PITCH_TYPES)].copy()


# -----------------------------
# 2. raw 무브먼트를 inch로 변환
# -----------------------------
data["ivb_raw_inches"] = data["pfx_z"] * 12.0
data["hb_raw_inches"] = data["pfx_x"] * 12.0


# -----------------------------
# 3. 암사이드가 양수가 되도록 수평 방향 통일
# -----------------------------
hand_factor = data["p_throws"].astype("string").str.upper().map(
    {"R": -12.0, "L": 12.0}
)
data["hb_arm_side_inches"] = data["pfx_x"] * hand_factor


# -----------------------------
# 4. 릴리스와 플레이트 도달 시각 및 체공시간
# -----------------------------
release_time = time_at_y(
    data["vy0"],
    data["ay"],
    data["release_pos_y"],
    reference_y=REFERENCE_Y_FT,
)
plate_time = time_at_y(
    data["vy0"],
    data["ay"],
    PLATE_Y_FT,
    reference_y=REFERENCE_Y_FT,
)

data["flight_time_s"] = plate_time - release_time
invalid_flight = (
    ~np.isfinite(data["flight_time_s"])
    | (data["flight_time_s"] <= 0)
)
data.loc[invalid_flight, "flight_time_s"] = np.nan


# -----------------------------
# 5. 학습기간의 유효 투구에서 구종별 기준시간 계산
# -----------------------------
valid_reference = (
    data["game_year"].isin(TRAIN_YEARS)
    & data["p_throws"].isin(["R", "L"])
    & data["flight_time_s"].between(
        MIN_FLIGHT_TIME_S, MAX_FLIGHT_TIME_S
    )
    & data[
        ["ivb_raw_inches", "hb_arm_side_inches", "flight_time_s"]
    ].notna().all(axis=1)
)

reference_times = (
    data.loc[valid_reference]
    .groupby("pitch_type", observed=True)["flight_time_s"]
    .median()
)

missing_reference = set(PITCH_TYPES).difference(reference_times.index)
if missing_reference:
    raise ValueError(
        "기준 체공시간을 계산할 수 없는 구종: "
        f"{sorted(missing_reference)}. TRAIN_YEARS와 표본을 확인하세요."
    )

data["reference_flight_time_s"] = data["pitch_type"].map(
    reference_times
)


# -----------------------------
# 6. 체공시간 보정계수 및 최종 무브먼트
# -----------------------------
valid_adjustment = (
    data["flight_time_s"].gt(0)
    & data["reference_flight_time_s"].gt(0)
)

data["flight_time_scale"] = np.where(
    valid_adjustment,
    (
        data["reference_flight_time_s"]
        / data["flight_time_s"]
    ) ** 2,
    np.nan,
)

data["ivb_ft"] = (
    data["ivb_raw_inches"] * data["flight_time_scale"]
)
data["hb_ft"] = (
    data["hb_arm_side_inches"] * data["flight_time_scale"]
)


# -----------------------------
# 7. 확인 및 저장
# -----------------------------
print("구종별 기준 체공시간(초)")
print(reference_times)

check_columns = [
    "pitch_type",
    "p_throws",
    "pfx_x",
    "pfx_z",
    "flight_time_s",
    "reference_flight_time_s",
    "flight_time_scale",
    "ivb_ft",
    "hb_ft",
]
print(data[check_columns].head())

data.to_csv(OUTPUT_PATH, index=False)
```

## 여러 연도·월 CSV를 한꺼번에 읽는 경우

2021–2023년 기준시간을 계산한 뒤 2024·2025년에 같은 척도를 적용하려면 먼저 모든 파일을 합친다.

```python
from pathlib import Path
import pandas as pd

files = sorted(Path("raw").glob("20*/statcast_*.csv"))
data = pd.concat(
    [pd.read_csv(file, low_memory=False) for file in files],
    ignore_index=True,
)
```

그 다음 위 코드의 무브먼트·체공시간 계산을 적용하고, `TRAIN_YEARS = [2021, 2022, 2023]`에서만 기준시간을 계산한다.

---

# 4부. 데이터 품질과 재현 시 주의사항

## 20. 결측값 처리

다음 중 하나라도 없으면 해당 투구의 보정 무브먼트를 계산하지 않는 것이 원칙이다.

- `pfx_x`, `pfx_z`
- `p_throws`
- `release_pos_y`
- `vy0`, `ay`
- 해당 구종의 $T_{ref}$

결측을 0으로 채우면 안 된다. 특히 `pfx_x = 0`과 `pfx_x` 결측은 완전히 다른 의미다.

## 21. 기준시간을 목표 자료에서 다시 계산하지 않기

서로 다른 연도나 리그를 비교하려면 하나의 학습자료에서 구한 $T_{ref,g}$를 고정해야 한다. 각 연도에서 기준시간을 새로 계산하면 결과가 서로 다른 척도를 갖게 된다.

2021–2025 연구 재현에서는:

- 기준시간 학습: 2021–2023
- 검증: 2024
- 최종 테스트: 2025

를 사용한다.

2021년 3월 파일 하나만 가지고 그 파일의 중앙값을 기준으로 계산할 수는 있지만, 이는 현재 연구의 2021–2023 기준값과 동일하지 않다. 이 경우 결과에 “해당 파일 내부 기준 체공시간 사용”이라고 명시해야 한다.

## 22. 현재 연구의 기준시간을 완전히 동일하게 재현할 때

위 Python 예제는 보정식에 꼭 필요한 최소 품질조건을 사용한다. 현재 프로젝트의 기준시간을 비트 단위까지 동일하게 재현하려면 원 연구의 `analysis_eligible` 조건도 적용해야 한다.

원 연구에서는 다음 조건을 추가로 사용했다.

- 정규시즌 `game_type = R`
- 구종 `FF`, `SI`, `FC`
- `description != pitchout`
- `p_throws`, `stand`가 `R` 또는 `L`
- `release_speed`, `release_extension`, `release_pos_y`, `arm_angle`, `pfx_x`, `pfx_z`, `vy0`, `ay`, `plate_x`, `plate_z` 모두 존재
- $0.25\le T\le0.60$초
- 학습자료에서 `release_speed`, raw IVB, 투수 기준 HB, `arm_angle`의 0.1%·99.9% 분위수 밖 추적 이상치 제외

팔 각도는 체공시간 보정식에 들어가지 않지만, 원 연구의 분석대상 및 기준시간 표본을 결정하는 품질관리에는 사용되었다. 팔 각도가 없는 raw 자료에서도 이 문서의 최소 계산법으로 보정값을 만들 수 있으나, 원 연구와 기준시간이 약간 달라질 수 있다.

## 23. 계산 후 필수 점검

최소한 다음 항목을 확인한다.

1. `flight_time_s`가 대부분 약 0.35~0.50초 범위인지 확인한다.
2. 릴리스 시각은 대체로 음수, 플레이트 시각은 양수인지 확인한다.
3. 우투수와 좌투수 모두 일반적인 암사이드 패스트볼의 `hb_arm_side_inches`가 양수인지 확인한다.
4. 구종별 `reference_flight_time_s`가 하나씩 존재하는지 확인한다.
5. $T=T_{ref}$ 부근에서 `flight_time_scale`이 1에 가까운지 확인한다.
6. `ivb_ft`, `hb_ft`에 무한대 또는 비정상적으로 큰 값이 없는지 확인한다.

## 24. 결과를 표현할 때의 정확한 명칭

`ivb_ft`, `hb_ft`는 실제로 공이 이동한 거리 그 자체가 아니다. 다음 명칭이 가장 정확하다.

> 구종별 기준 체공시간으로 환산한 무브먼트

또는:

> reference-time-equivalent movement

이 값은 일정한 평균 횡가속도라는 근사 아래, 모든 투구에 동일한 움직임 기회를 부여한 비교척도다. 팔 각도, 회전축, 공기밀도, 구장 환경까지 제거한 완전한 물리 보정값은 아니다.

---

# 5부. 현재 프로젝트의 구현 위치

- raw 무브먼트와 체공시간 보정: `src/fastball_movement/features/movement.py`
- 체공시간 계산: `src/fastball_movement/features/flight_time.py`
- 수평 방향 통일: `src/fastball_movement/features/horizontal.py`
- 품질관리: `src/fastball_movement/data/quality.py`
- 설정값: `configs/analysis.yaml`

핵심 계산 흐름은 다음과 같다.

$$
(pfx_x,pfx_z)
\xrightarrow{\times12}
\text{인치 단위 raw 무브먼트}
$$

$$
pfx_x+p\_throws
\longrightarrow
\text{암사이드 양수 방향 통일}
$$

$$
(release\_pos\_y,vy0,ay)
\longrightarrow
T_i
$$

$$
(pitch\_type,game\_year,T_i)
\longrightarrow
T_{ref,g}
$$

$$
\boxed{
M_{FT,i}
=M_i
\left(\frac{T_{ref,g}}{T_i}\right)^2
}
$$
