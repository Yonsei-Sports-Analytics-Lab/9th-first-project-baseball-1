# `statcast_2021-03.csv` 변수 설명서

## 1. 데이터 개요

- 대상 파일: `raw/2021/statcast_2021-03.csv`
- 관측 단위: **한 행 = 한 투구(pitch)**. 단, 투구 추적에 실패했거나 실제 투구가 아닌 기록도 있어 투구 물리량이 비어 있을 수 있다.
- 범위: 2021-03-15 ~ 2021-03-30, 스프링 트레이닝(`game_type = S`)
- 크기: 42,561행, 119개 변수
- 좌표 관점: 투구·홈플레이트 위치는 기본적으로 **포수 시점**이다. 거리 단위는 별도 표시가 없으면 변수별 설명을 따른다.
- 빈칸: 대부분 “0”이 아니라 **측정되지 않음 / 해당 상황이 아님 / 구 스키마 또는 신 스키마 변수**라는 뜻이다.

`값 존재`는 이 파일 42,561행 중 빈칸이 아닌 행 수다. 타구 변수는 타자가 공을 쳤을 때만, 타석 결과 변수는 그 타석의 마지막 투구에 주로 기록되므로 빈칸이 정상이다.

## 2. 변수 사전

### 2.1 투구·경기 식별 및 결과

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `pitch_type` | 범주 코드 | Statcast가 분류한 구종 코드. 코드표는 아래 참조. | 23,757 |
| `game_date` | 날짜 | 경기 날짜. | 42,561 |
| `player_name` | 문자열 | 검색 대상 선수 이름. 이 파일처럼 전체 투구를 받은 자료에서는 일반적으로 투수 이름이며 `성, 이름` 형식이다. | 42,561 |
| `batter` | MLBAM ID | 타자의 MLB 고유 선수 ID. 숫자이지만 계산용 수치가 아니라 식별자다. | 42,561 |
| `pitcher` | MLBAM ID | 투수의 MLB 고유 선수 ID. | 42,561 |
| `events` | 범주 | 해당 투구로 끝난 **타석의 최종 결과**(안타, 삼진, 볼넷 등). 타석 도중 투구에는 보통 빈칸이다. | 15,955 |
| `description` | 범주 | **이 투구 자체의 결과**(볼, 헛스윙, 파울, 인플레이 등). | 42,561 |
| `zone` | 정수 코드 | 공이 홈플레이트를 지날 때의 Gameday 구역. 보통 1~9는 스트라이크존 내부 3×3, 11~14는 존 바깥 구역이다. | 23,757 |
| `des` | 문자열 | Gameday의 사람이 읽는 플레이 설명 문장. 대개 타석 종료 행에 있다. | 15,924 |
| `game_type` | 범주 코드 | 경기 종류. 이 파일은 모두 `S`(스프링 트레이닝). | 42,561 |
| `stand` | L/R | 타자의 타석 방향: `L` 좌타, `R` 우타. | 42,561 |
| `p_throws` | L/R | 투수의 투구 손: `L` 좌투, `R` 우투. | 42,561 |
| `home_team` | 팀 코드 | 홈팀 약어. | 42,561 |
| `away_team` | 팀 코드 | 원정팀 약어. | 42,561 |
| `type` | B/S/X | 투구 결과의 대분류: `B` 볼, `S` 스트라이크, `X` 인플레이. 파울·헛스윙 등도 `S`에 포함될 수 있다. | 42,561 |
| `hit_location` | 1~9 | 타구를 처음 처리한 수비수의 포지션 번호. 1 투수, 2 포수, 3 1루수, 4 2루수, 5 3루수, 6 유격수, 7 좌익수, 8 중견수, 9 우익수. | 13,753 |
| `bb_type` | 범주 | 타구 유형: 땅볼, 라인드라이브, 뜬공, 팝업. | 10,298 |
| `game_year` | 연도 | 경기 시즌 연도. 이 파일은 모두 2021. | 42,561 |
| `game_pk` | ID | 경기의 MLB 고유 ID. 한 경기의 모든 투구에서 같다. | 42,561 |
| `at_bat_number` | 정수 | 해당 경기에서 타석이 발생한 순번. `game_pk`와 함께 타석 식별에 유용하다. | 42,561 |
| `pitch_number` | 정수 | 해당 타석 안에서 몇 번째 투구인지 나타내는 번호. | 42,561 |
| `pitch_name` | 문자열 | `pitch_type`의 사람이 읽는 구종명. | 23,757 |

### 2.2 볼카운트·주자·이닝·점수

모두 특별한 언급이 없으면 **해당 투구 직전** 상태다. `post_*`만 투구 직후 상태다.

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `balls` | 정수 | 투구 전 볼 개수(0~3). | 42,561 |
| `strikes` | 정수 | 투구 전 스트라이크 개수(0~2). | 42,561 |
| `on_3b` | MLBAM ID | 투구 전 3루 주자의 선수 ID. 주자가 없으면 빈칸. | 4,664 |
| `on_2b` | MLBAM ID | 투구 전 2루 주자의 선수 ID. | 8,675 |
| `on_1b` | MLBAM ID | 투구 전 1루 주자의 선수 ID. | 12,747 |
| `outs_when_up` | 정수 | 투구 전 아웃 수(0~2). | 42,561 |
| `inning` | 정수 | 이닝 번호. | 42,561 |
| `inning_topbot` | Top/Bot | `Top` 초, `Bot` 말. | 42,561 |
| `home_score` | 점수 | 투구 전 홈팀 득점. | 42,561 |
| `away_score` | 점수 | 투구 전 원정팀 득점. | 42,561 |
| `bat_score` | 점수 | 투구 전 공격팀 득점. | 42,561 |
| `fld_score` | 점수 | 투구 전 수비팀 득점. | 42,561 |
| `post_away_score` | 점수 | 투구 후 원정팀 득점. | 42,561 |
| `post_home_score` | 점수 | 투구 후 홈팀 득점. | 42,561 |
| `post_bat_score` | 점수 | 투구 후 공격팀 득점. | 42,561 |
| `post_fld_score` | 점수 | 투구 후 수비팀 득점. | 42,561 |
| `home_score_diff` | 점수 차 | 투구 전 `home_score - away_score`. 양수면 홈팀 리드. | 42,561 |
| `bat_score_diff` | 점수 차 | 투구 전 `bat_score - fld_score`. 양수면 공격팀 리드. | 42,561 |

### 2.3 투구 위치·속도·움직임·회전

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `release_speed` | mph | 손을 떠날 때 기준 투구 속도. 2017년 이후 Statcast 측정값이다. | 23,757 |
| `release_pos_x` | ft | 릴리스 순간 공의 좌우 위치(포수 시점). | 23,757 |
| `release_pos_z` | ft | 릴리스 순간 공의 지면 기준 높이. | 23,757 |
| `pfx_x` | ft | 공의 좌우 무브먼트(포수 시점). 공이 회전·공기력 때문에 얼마나 옆으로 움직였는지를 나타내는 계열의 값이다. | 23,757 |
| `pfx_z` | ft | 공의 수직 무브먼트. 일반적으로 중력만 받는 궤적과 비교한 수직 변화(유도 수직 무브먼트)로 해석하며, 실제 낙하량과 다르다. | 23,757 |
| `plate_x` | ft | 공이 홈플레이트 앞면을 지날 때 중심선으로부터의 좌우 위치(포수 시점). 2021 자료이므로 앞면 기준이다. | 23,757 |
| `plate_z` | ft | 공이 홈플레이트 앞면을 지날 때 지면으로부터의 높이. | 23,757 |
| `vx0` | ft/s | 홈플레이트에서 50ft 지점의 초기 x축(좌우) 속도 성분. | 23,757 |
| `vy0` | ft/s | 50ft 지점의 초기 y축(마운드↔홈 방향) 속도 성분. 공이 홈 쪽으로 오므로 보통 음수다. | 23,757 |
| `vz0` | ft/s | 50ft 지점의 초기 z축(수직) 속도 성분. | 23,757 |
| `ax` | ft/s² | 50ft 지점에서 추정한 x축 가속도 성분. | 23,757 |
| `ay` | ft/s² | 50ft 지점에서 추정한 y축 가속도 성분. | 23,757 |
| `az` | ft/s² | 50ft 지점에서 추정한 z축 가속도 성분. 중력과 공기력의 영향을 함께 받는다. | 23,757 |
| `sz_top` | ft | 해당 타자의 스트라이크존 위쪽 경계. 2021년에는 경기 운영자가 설정한 값이다. | 23,757 |
| `sz_bot` | ft | 해당 타자의 스트라이크존 아래쪽 경계. | 23,757 |
| `effective_speed` | mph | 릴리스 익스텐션 등을 반영한 체감 속도. 실제 레이더건 속도인 `release_speed`와 다르다. | 23,733 |
| `release_spin_rate` | rpm | 릴리스 시 공의 회전수. | 23,755 |
| `release_extension` | ft | 투수가 마운드 쪽 기준점에서 홈 쪽으로 얼마나 앞에서 공을 놓았는지 나타내는 익스텐션. | 23,734 |
| `release_pos_y` | ft | 홈플레이트 기준 릴리스 지점까지의 전후 거리. 익스텐션이 클수록 일반적으로 작아진다. | 23,757 |
| `spin_axis` | degree | x-z 평면에 투영한 회전축 방향(0~360°). 180°는 순수 백스핀 패스트볼, 0°는 순수 탑스핀 12–6 커브에 해당한다. | 23,755 |
| `api_break_z_with_gravity` | inch | 중력에 의한 낙하까지 포함한 수직 브레이크. `pfx_z`와 정의가 다르므로 직접 혼용하면 안 된다. | 23,757 |
| `api_break_x_arm` | inch | 투수의 **팔 쪽(arm side)**을 기준으로 부호 방향을 맞춘 수평 브레이크. 좌·우투 비교에 편리하다. | 23,757 |
| `api_break_x_batter_in` | inch | 타자의 몸쪽 방향을 기준으로 표현한 수평 브레이크. 타자 좌·우에 따라 부호 해석이 달라진다. | 23,757 |
| `arm_angle` | degree | 지면과 평행한 선과, 투구 어깨에서 릴리스 공 위치로 그은 선 사이의 팔 각도. | 0 |

> `pfx_x`, `pfx_z`는 ft이므로 인치로 바꾸려면 12를 곱한다. 반면 `api_break_*`는 이미 inch다.

### 2.4 타구 위치·타구 질

이 변수들은 공이 맞았고 추적이 성공한 행에만 주로 존재한다.

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `hc_x` | 도식 좌표 | Baseball Savant 필드 그림 위 타구 위치의 x좌표. 실제 ft 좌표가 아니므로 물리 거리로 바로 해석하지 않는다. | 10,283 |
| `hc_y` | 도식 좌표 | 필드 그림 위 타구 위치의 y좌표. | 10,283 |
| `hit_distance_sc` | ft | Statcast가 추정한 타구 비거리. 모든 타구에 생기는 값은 아니다. | 7,272 |
| `launch_speed` | mph | 타구의 출구 속도(Exit Velocity, EV). | 7,271 |
| `launch_angle` | degree | 타구 발사각. 0°는 수평, 양수는 위쪽, 음수는 아래쪽 방향이다. | 7,273 |
| `launch_speed_angle` | 1~6 | 출구속도와 발사각 조합으로 분류한 타구 질 구역. 코드표는 아래 참조. | 3,894 |
| `hyper_speed` | mph | Savant의 Adjusted EV. 각 타구의 EV가 88mph보다 낮으면 88, 아니면 실제 EV를 쓰는 보정값, 즉 `max(88, launch_speed)`다. | 7,340 |

### 2.5 기대 성적·플레이 가치

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `estimated_ba_using_speedangle` | 0~1 | 출구속도와 발사각에 근거한 기대 타율(xBA). 수비 결과가 아니라 비슷한 타구가 안타가 될 확률에 가깝다. | 0 |
| `estimated_woba_using_speedangle` | 가중값 | 출구속도와 발사각에 근거한 기대 wOBA(xwOBA). | 0 |
| `woba_value` | 가중값 | 실제 타석 결과에 부여된 wOBA 분자 가중치. 타석 종료 행에 기록된다. | 15,955 |
| `woba_denom` | 0/1 성격 | 해당 타석 결과가 wOBA 분모에 들어가는지를 나타내는 값. | 0 |
| `babip_value` | 0/1 | 실제 타석 결과가 BABIP 계산에서 안타면 1, 아웃이면 0인 행 수준 값. 삼진·홈런·볼넷 등 BABIP 비대상 결과의 처리에 유의한다. | 15,955 |
| `iso_value` | 수치 | 실제 타석 결과의 ISO 기여값. 보통 단타/아웃 0, 2루타 1, 3루타 2, 홈런 3으로 장타의 추가 루타를 표현한다. | 15,955 |
| `estimated_slg_using_speedangle` | 0 이상 | 출구속도와 발사각에 근거한 기대 장타율(xSLG) 값. | 0 |
| `delta_home_win_exp` | 확률 변화 | 플레이 전후 홈팀 승리확률 변화. 양수면 홈팀에 유리, 음수면 불리하다. 공식 CSV 문서는 타석 전후 변화로 설명한다. | 42,561 |
| `delta_run_exp` | 기대 득점 변화 | 투구 전후 공격팀의 기대 득점 변화. 양수면 타자/공격팀에 유리하다. | 42,553 |
| `delta_pitcher_run_exp` | 기대 득점 변화 | 투수 관점의 기대 득점 가치. 이 파일에서는 보통 `-delta_run_exp`다. 양수면 투수에게 유리하다. | 42,553 |
| `home_win_exp` | 0~1 | 투구 전 홈팀 승리확률. | 42,561 |
| `bat_win_exp` | 0~1 | 투구 전 공격팀 승리확률. | 42,561 |

### 2.6 수비 배치와 야수

`fielder_2`~`fielder_9`는 **투구 직전 해당 수비 포지션에 있던 선수**의 MLBAM ID다.

| 변수 | 포지션·형식 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `fielder_2` | 포수 ID | 포수. | 42,561 |
| `fielder_3` | 1루수 ID | 1루수. | 42,561 |
| `fielder_4` | 2루수 ID | 2루수. | 42,561 |
| `fielder_5` | 3루수 ID | 3루수. | 42,561 |
| `fielder_6` | 유격수 ID | 유격수. | 42,561 |
| `fielder_7` | 좌익수 ID | 좌익수. | 42,561 |
| `fielder_8` | 중견수 ID | 중견수. | 42,561 |
| `fielder_9` | 우익수 ID | 우익수. | 42,561 |
| `if_fielding_alignment` | 범주 | 투구 시점 내야 수비 배치. 이 파일의 값은 `Standard`, `Infield shift`, `Infield shade`, `Strategic`. | 22,978 |
| `of_fielding_alignment` | 범주 | 투구 시점 외야 수비 배치. 이 파일의 값은 `Standard`, `Strategic`, `4th outfielder`. | 22,978 |

### 2.7 연령·타순 상대 횟수·경기 간 휴식

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `age_pit_legacy` | 세 | 기존 방식으로 계산한 투수 나이: 해당 연도 6월 30일 기준. | 42,561 |
| `age_bat_legacy` | 세 | 기존 방식으로 계산한 타자 나이: 해당 연도 6월 30일 기준. | 42,561 |
| `age_pit` | 세 | 해당 연도 12월 31일 기준 투수 나이. | 42,561 |
| `age_bat` | 세 | 해당 연도 12월 31일 기준 타자 나이. | 42,561 |
| `n_thruorder_pitcher` | 정수 | 이 투수가 그 경기에서 타선을 몇 번째 순환하며 상대 중인지 나타내는 횟수. 보통 1은 첫 번째 대면 순환이다. | 42,561 |
| `n_priorpa_thisgame_player_at_bat` | 정수 | 해당 타자가 이 경기에서 앞서 완료한 타석 수. 0이면 첫 타석이다. | 42,561 |
| `pitcher_days_since_prev_game` | 일 | 투수의 직전 출전 경기 이후 지난 일수. | 0 |
| `batter_days_since_prev_game` | 일 | 타자의 직전 출전 경기 이후 지난 일수. | 0 |
| `pitcher_days_until_next_game` | 일 | 투수의 다음 출전 경기까지 남은 일수. 미래 정보를 포함하므로 예측 모델에서는 데이터 누출에 유의한다. | 0 |
| `batter_days_until_next_game` | 일 | 타자의 다음 출전 경기까지 남은 일수. 역시 미래 정보다. | 0 |

### 2.8 배트·스윙 추적

이 열들은 최신 Statcast CSV 스키마에 포함되지만 이 **2021년 파일에서는 전부 비어 있다**. 2021 분석의 입력 변수로 바로 사용할 수 없다.

| 변수 | 형식·단위 | 쉬운 설명 | 값 존재 |
|---|---|---|---:|
| `bat_speed` | mph | 임팩트와 관련된 배트 스위트스폿의 스윙 속도. | 0 |
| `swing_length` | ft | 추적 시작부터 임팩트까지 배트 헤드가 3차원 공간에서 이동한 총 거리. | 0 |
| `miss_distance` | inch | 헛스윙 때 배트와 공 사이의 최소 거리. | 0 |
| `attack_angle` | degree | 임팩트 순간 배트 스위트스폿 진행 방향의 수직 각도(지면 기준). | 0 |
| `attack_direction` | degree | 임팩트 순간 배트 진행 방향의 수평 각도(홈에서 중견수 방향선을 기준). | 0 |
| `swing_path_tilt` | degree | 임팩트 전 40ms 동안 배트 경로가 만드는 스윙 평면의 수직 기울기. 값이 클수록 더 가파른 스윙으로 해석한다. | 0 |
| `intercept_ball_minus_batter_pos_x_inches` | inch | 배트-공 충돌점과 타자 무게중심 사이의 x축(좌우) 거리. | 0 |
| `intercept_ball_minus_batter_pos_y_inches` | inch | 충돌점과 타자 무게중심 사이의 y축(마운드-홈 방향) 거리, 즉 타자 기준 타격 깊이와 관련된 값. | 0 |

### 2.9 폐기되었거나 이 파일에서 사용되지 않는 열

아래 열은 모두 42,561행 전체가 빈칸이다. 분석에서는 제거해도 이 파일의 정보가 사라지지 않는다.

| 변수 | 설명 | 대체·주의 |
|---|---|---|
| `spin_dir` | 구 추적 시스템의 회전 방향 변수(폐기). | 현재 회전 방향 분석에는 `spin_axis` 사용. |
| `spin_rate_deprecated` | 구 회전수 변수(폐기). | `release_spin_rate` 사용. |
| `break_angle_deprecated` | 구 브레이크 각도 변수(폐기). | 목적에 따라 `pfx_*` 또는 `api_break_*` 사용. |
| `break_length_deprecated` | 구 브레이크 길이 변수(폐기). | 목적에 따라 `pfx_*` 또는 `api_break_*` 사용. |
| `tfs_deprecated` | 구 추적 시스템 시간 필드(폐기). | 사용하지 않음. |
| `tfs_zulu_deprecated` | 구 추적 시스템 UTC 시간 필드(폐기). | 사용하지 않음. |
| `umpire` | 구 스키마의 심판 필드(폐기). | 이 파일에서는 심판 정보 없음. |
| `sv_id` | 경기 안의 플레이 이벤트용 비고유 ID였던 필드. | 이 파일에서는 비어 있으며 안정적인 키로 사용하면 안 됨. |

## 3. 코드값 해석

### `pitch_type` / `pitch_name`

이 파일에서 실제 확인된 11개 구종이다.

| 코드 | 구종명 | 한국어 |
|---|---|---|
| `FF` | 4-Seam Fastball | 포심 패스트볼 |
| `SI` | Sinker | 싱커 |
| `FC` | Cutter | 커터 |
| `SL` | Slider | 슬라이더 |
| `ST` | Sweeper | 스위퍼 |
| `SV` | Slurve | 슬러브 |
| `CU` | Curveball | 커브볼 |
| `KC` | Knuckle Curve | 너클 커브 |
| `CS` | Slow Curve | 슬로 커브 |
| `CH` | Changeup | 체인지업 |
| `FS` | Split-Finger | 스플리터 |

구종 분류는 Statcast 알고리즘의 결과이므로 실제 그립 또는 투수의 명명과 항상 같지는 않다.

### `description`

| 값 | 의미 |
|---|---|
| `automatic_ball` | 자동 볼 판정 |
| `ball` | 볼 |
| `blocked_ball` | 포수가 블로킹한 볼 |
| `called_strike` | 루킹 스트라이크 |
| `foul` | 파울 |
| `foul_bunt` | 번트 파울 |
| `foul_tip` | 파울팁 |
| `hit_by_pitch` | 몸에 맞는 공 |
| `hit_into_play` | 타구가 인플레이됨 |
| `missed_bunt` | 번트 헛스윙 |
| `swinging_strike` | 헛스윙 스트라이크 |
| `swinging_strike_blocked` | 포수가 블로킹한 헛스윙 스트라이크 |

### `bb_type`

| 값 | 의미 |
|---|---|
| `ground_ball` | 땅볼 |
| `line_drive` | 라인드라이브 |
| `fly_ball` | 뜬공 |
| `popup` | 내야성 높은 팝업 |

### `launch_speed_angle`

| 코드 | Statcast 분류 | 쉬운 뜻 |
|---:|---|---|
| 1 | Weak | 약한 타구 |
| 2 | Topped | 공 윗부분을 때린 타구 |
| 3 | Under | 공 아랫부분을 때린 타구 |
| 4 | Flare/Burner | 빗맞은 안타성 플레어 또는 낮고 빠른 타구 |
| 5 | Solid Contact | 강한 정타 |
| 6 | Barrel | 이상적인 출구속도·발사각 조합의 배럴 타구 |

### `events`

이 파일에서 확인된 값은 다음과 같다. 이름 그대로 해석하되, `events`는 개별 투구가 아니라 **완료된 타석 결과**라는 점이 중요하다.

`catcher_interf`, `double`, `double_play`, `field_error`, `field_out`, `fielders_choice`, `fielders_choice_out`, `force_out`, `grounded_into_double_play`, `hit_by_pitch`, `home_run`, `intent_walk`, `sac_bunt`, `sac_fly`, `sac_fly_double_play`, `single`, `strikeout`, `strikeout_double_play`, `triple`, `triple_play`, `truncated_pa`, `walk`

- `truncated_pa`: 투수 교체, 주자 아웃 등으로 정상적인 타석 결과 없이 끊긴 타석.
- `fielders_choice`: 야수선택으로 타자는 출루한 결과.
- `fielders_choice_out`: 야수선택 과정에서 기록상 아웃이 발생한 결과.

## 4. 분석할 때 특히 주의할 점

1. **행 수와 투구 추적 성공 수가 다르다.** 전체 42,561행 중 `pitch_type`, `release_speed`, `pfx_*` 등이 있는 행은 23,757행이다. 물리량 분석 전에 결측 원인을 확인하고 필요한 모집단을 명시해야 한다.
2. **0과 빈칸은 다르다.** 예를 들어 `on_1b` 빈칸은 1루 주자 없음이고, `launch_speed` 빈칸은 타구가 없었거나 추적되지 않은 것이다.
3. **ID는 숫자형 범주다.** `batter`, `pitcher`, `fielder_*`, `on_*`, `game_pk`를 평균내거나 표준화하지 않는다.
4. **`events`는 타석 종료 행만 사용한다.** 타석 수나 결과를 셀 때는 `events`가 비어 있지 않은 행을 사용한다. 모든 투구 행에서 집계하면 같은 타석을 여러 번 셀 수 있다.
5. **타구 표본은 더 작다.** `bb_type` 10,298행, `launch_speed` 7,271행으로 서로도 측정 가능 행 수가 다르다.
6. **움직임 단위가 다르다.** `pfx_*`는 ft, `api_break_*`는 inch다. 수직 정의도 `pfx_z`와 `api_break_z_with_gravity`가 다르다.
7. **좌우 방향 정규화 여부가 다르다.** `pfx_x`는 포수 시점 좌표지만 `api_break_x_arm`은 투수의 팔 쪽, `api_break_x_batter_in`은 타자 몸쪽 기준이다.
8. **기대 지표가 이 파일에는 없다.** `estimated_ba_using_speedangle`, `estimated_woba_using_speedangle`, `estimated_slg_using_speedangle`은 열만 있고 전부 빈칸이다.
9. **신규 배트 추적 지표도 전부 빈칸이다.** 2021년 분석에서 0으로 채우면 안 된다.
10. **미래 정보 변수에 유의한다.** `*_days_until_next_game`는 모델 학습 시 미래 데이터 누출을 일으킬 수 있다(이 파일에서는 전부 빈칸).

## 5. 자주 쓰는 분석 키와 예시

- 경기 식별: `game_pk`
- 타석 식별: 보통 `game_pk + at_bat_number`
- 투구 식별: 보통 `game_pk + at_bat_number + pitch_number`
- 투수별 분석: `pitcher`를 키로 사용하고 `player_name`은 표시용으로 사용
- 타자별 분석: `batter`
- 패스트볼 무브먼트 분석 예: `pitch_type`, `release_speed`, `pfx_x`, `pfx_z`, `release_spin_rate`, `spin_axis`, `release_extension`, `release_pos_x`, `release_pos_z`

키 조합은 실무적으로 유용하지만 MLB가 데이터베이스 기본키로 명시한 것은 아니므로, 병합 후 중복을 점검하는 것이 안전하다.

## 6. 출처와 해석 기준

- MLB Baseball Savant, [Statcast Search CSV Documentation](https://baseballsavant.mlb.com/csv-docs) — 변수의 공식 정의와 단위
- MLB Baseball Savant의 같은 문서 내 Statcast Glossary — EV, 발사각, 배트 추적 및 투구 지표 정의
- 실제 파일을 직접 점검해 기간, 행 수, 열 수, 범주값과 각 열의 비결측 행 수를 기록함

공식 문서가 간략하거나 현재 스키마와 2021년 자료의 제공 상태가 다른 항목은 변수명, 실제 값, 야구 통계의 일반적 계산법을 함께 사용해 설명했다. 따라서 `hc_x/hc_y`, 폐기 변수, 값이 전부 빈 신규 변수는 물리량으로 임의 해석하지 않는 것이 안전하다.
